import { StoreOptions } from './store-options';

/**
 * Interface defining the parameters required for clearing all items from the store.
 */
export interface ClearItemsParams {
    /**
     * Store-specific options
     */
    options: StoreOptions;
} 